name: kaizen
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #Firebase Dependencies
  firebase_core: ^1.3.0
  firebase_auth: ^2.0.0
  cloud_firestore: ^2.3.0

  google_fonts: ^2.1.0
  rxdart: ^0.23.1

  flutter_spinkit: ^5.0.0 #Loading Spinners

  dropdownfield: ^1.0.3
  animated_icon_button: ^1.0.2

  slimy_card: ^1.0.4 # Slimy Card
  uuid: ^3.0.4 # UUID

  email_validator: ^2.0.1 # Email Validator
  password_strength: ^0.2.0 # Password Strength

  awesome_dialog: #Error Message
  achievement_view: ^0.2.1 #Achievement View

  shared_preferences: ^2.0.6 #Shared Preferences
  font_awesome: ^5.10.1 # Icons
  flutter_staggered_animations: "^1.0.0" #List Animation
  animator: ^3.1.0 #Icon Animator

  flip_card: ^0.5.0 #Flip Card Animation
  animations: ^2.0.1 #Page Transition Animation
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/Registration/
    - assets/Background/
